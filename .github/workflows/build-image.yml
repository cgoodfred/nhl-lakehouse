name: Build image

on:
  push:
    branches: [main]
    paths:
      - 'ingest/**'
      - '.github/workflows/build-image.yml'
  workflow_dispatch:

permissions:
  contents: read
  packages: write
  pull-requests: write

concurrency:
  group: build-ingest-image
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-qemu-action@v3

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/build-push-action@v6
        with:
          context: ingest
          file: ingest/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ghcr.io/cgoodfred/nhl-lakehouse/ingest:latest
            ghcr.io/cgoodfred/nhl-lakehouse/ingest:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  open-image-bump-pr:
    needs: build
    runs-on: ubuntu-latest
    concurrency:
      group: ingest-image-bump-pr
      cancel-in-progress: false
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: opentofu/setup-opentofu@v1
        with:
          tofu_wrapper: false

      - name: Update ingest image tag
        id: update
        env:
          IMAGE: ghcr.io/cgoodfred/nhl-lakehouse/ingest:${{ github.sha }}
        run: |
          perl -0pi -e 's#image\s+=\s+"ghcr\.io/cgoodfred/nhl-lakehouse/ingest:[^"]+"#image = "$ENV{IMAGE}"#' infra/ingest-backfill.tf

          if ! grep -Fq "$IMAGE" infra/ingest-backfill.tf; then
            echo "::error::image substitution did not update infra/ingest-backfill.tf"
            exit 1
          fi

          tofu fmt infra/ingest-backfill.tf

          if git diff --quiet -- infra/ingest-backfill.tf; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          echo "changed=true" >> "$GITHUB_OUTPUT"

      - name: Open image bump PR
        if: steps.update.outputs.changed == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
          BRANCH: chore/pin-ingest-image
          SHORT_SHA: ${{ github.sha }}
        run: |
          SHORT_SHA="${SHORT_SHA:0:7}"

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git switch -c "$BRANCH"
          git add infra/ingest-backfill.tf
          git commit -m "chore(infra): pin ingest image to ${SHORT_SHA}"
          git push --force-with-lease origin "$BRANCH"

          if gh pr view "$BRANCH" --json state --jq '.state == "OPEN"' 2>/dev/null | grep -q true; then
            gh pr edit "$BRANCH" \
              --title "chore(infra): pin ingest image to ${SHORT_SHA}" \
              --body "Pins the ingest backfill Job image to the image built from ${GITHUB_SHA}."
          else
            gh pr create \
              --base main \
              --head "$BRANCH" \
              --title "chore(infra): pin ingest image to ${SHORT_SHA}" \
              --body "Pins the ingest backfill Job image to the image built from ${GITHUB_SHA}."
          fi
